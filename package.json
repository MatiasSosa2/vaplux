{
  "name": "vaplux-next",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "echo 'Use next build with output: export'",
    "export:ghpages": "powershell -NoProfile -Command \"if (Test-Path docs) { Remove-Item -Recurse -Force docs }; Copy-Item -Recurse -Force out docs; New-Item -ItemType File -Force docs/.nojekyll\"",
    "preview": "npx serve -s out"
  },
  "dependencies": {
    "@radix-ui/react-dialog": "^1.1.15",
    "@radix-ui/react-tooltip": "^1.2.8",
    "framer-motion": "^12.23.26",
    "next": "14.2.5",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.33",
    "tailwindcss": "^3.4.10"
  }
}
